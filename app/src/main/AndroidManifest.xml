<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="systmorder.apporder">

    <supports-screens android:smallScreens="true"
    android:normalScreens="true"
    android:largeScreens="true"
    android:xlargeScreens="true"
    android:anyDensity="true" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AllLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminMainActivity"></activity>
        <activity android:name=".OwnerChooseActivity"></activity>
        <activity android:name=".OwnerMainActivity"></activity>
        <activity android:name=".AllRegisterActivity"></activity>
        <!--<activity android:name=".AdminTest">-->

        <!--</activity>-->
    </application>

</manifest>