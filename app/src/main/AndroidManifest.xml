<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="systmorder.apporder">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminLoginActivity"></activity>
        <activity android:name=".AllLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AllForgotPasswordActivity" />
        <activity android:name=".AllRegisterActivity" />

        <activity android:name=".AdminMainActivity" />
        <activity android:name=".OwnerChooseActivity" />
        <activity android:name=".OwnerMainActivity" />

        <activity android:name=".StaffChooseActivity" />
        <activity android:name=".StaffMainActivity" />
        <activity android:name=".StaffMainActivityKitchenView"/>

        <activity android:name=".CustMainActivity" />
        <activity android:name=".CustChooseRestaurant"/>
        <activity android:name=".AnyOrientationCaptureActivity"
            android:screenOrientation="fullSensor"
            android:stateNotNeeded="true"
            android:theme="@style/zxing_CaptureTheme"
            android:windowSoftInputMode="stateAlwaysHidden"/>
        <activity android:name=".CustOrderListActivity"/>

         <activity android:name=".AdminTest">


         </activity>
    </application>

</manifest>